# Copy this file to .env and fill in real values. Never commit .env.

# Used for embeddings (text-embedding-3-small). Also a likely candidate
# for phase 4's Socratic dialogue LLM, but that choice isn't final yet.
OPENAI_API_KEY=
